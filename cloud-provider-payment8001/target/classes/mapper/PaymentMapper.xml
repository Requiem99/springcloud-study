<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dwg.Mapper.PaymentMapper">
    <select id="getPaymentList"  resultType="Payment">
        select * from payment
    </select>

    <select id="findPaymentById" parameterType="long" resultType="Payment">
        select * from payment where id = #{id}
    </select>

    <insert id="createPayment" parameterType="Payment" useGeneratedKeys="true" keyProperty="id">
        insert into payment (serial) values (#{serial})
    </insert>
</mapper>